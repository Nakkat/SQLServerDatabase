CREATE DATABASE EquipeFutebol
CREATE TABLE Equipe (
	IdEquipe INT IDENTITY PRIMARY KEY NOT NULL,
	Nome VARCHAR(30),
);
CREATE TABLE Jogador (
	IdJogagador INT IDENTITY PRIMARY KEY NOT NULL,
	Nome VARCHAR(100),
	Idade INT,
	IdEquipe INT FOREIGN KEY REFERENCES EQUIPE (IdEquipe),
);



